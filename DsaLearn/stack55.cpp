#include<iostream>
#include<stack>
using namespace std ;

int main(){

    //reverse a string using a stack 
    /*string str = " mrigangka" ;
    stack<char>s ;
    for(int i = 0 ; i<str.size();i++){
        char ch = str[i] ;
        s.push(ch) ;
    }

    string ans = "" ;
    while(!s.empty()){
        // char ch = s.top() ;
        cout<<s.top() ;
        // ans.push_back(ch) ;
        s.pop() ;
    } */

   
    
    return 0 ;
}